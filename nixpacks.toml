[build]
baseImage = "ubuntu:22.04"

[phases.setup]
cmds = [
    "apt-get update",
    "apt-get install -y python3 python3-pip",
    "pip3 install -r requirements.txt"
]

[start]
cmd = "python3 manage.py migrate && gunicorn tu_proyecto.wsgi:application --bind 0.0.0.0:$PORT"