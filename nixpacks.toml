[build]
baseImage = "python:3.11-slim"

[phases.setup]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python manage.py migrate && gunicorn api_calc_cal.wsgi:application --bind 0.0.0.0:$PORT"