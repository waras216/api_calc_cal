[phases.setup]
nixPkgs = [
    "python3",
    "mariadb" 
]

[phases.install]
# CORRECCIÓN: Cambia a "python -m pip" para asegurar que pip sea encontrado.
cmds = ["python -m pip install -r requirements.txt"]

[start]
cmd = "gunicorn backend_calories_calc.wsgi:application --bind 0.0.0.0:$PORT"