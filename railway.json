{
    "build":{
        "builder": "NIXPACKS"
    },
    "deploy":{
        "startcommands": "python manage.py migrate && gunicorn backend_calories_calc.wsgi:application --bind 0.0.0.0:$PORT"
    }
}